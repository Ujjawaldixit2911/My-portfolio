{
  "name": "portfolio-root",
  "version": "1.0.0",
  "description": "Root orchestrator for Ujjawal Dixit's React + Express Portfolio",
  "main": "index.js",
  "scripts": {
    "install-all": "npm install && npm install --prefix frontend && npm install --prefix backend",
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
